import React from "react";

interface Props {}

const LectureList = (props: Props) => {
    return (
        <div title="lectureList">
            <h1>강의 목록</h1>
        </div>
    );
};

export default LectureList;
